language: python
python: 3.6
install:
  - "pip install Werkzeug==1.0.1"
  - "pip install Lektor"
script: "lektor build"
deploy:
  provider: script
  script: "lektor deploy ghpages"
  on:
    branch: develop
